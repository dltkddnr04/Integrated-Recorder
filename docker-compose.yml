services:
  archiver:
    build: .
    ports:
      - "8080:8080"
    environment:
      ADDR: ":8080"
      DATA_DIR: /data
      ADAPTER_DIR: /adapters
    volumes:
      - ./data:/data
    restart: unless-stopped
